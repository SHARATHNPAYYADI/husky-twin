# Local dev doesn't need this file — client.ts already falls back to
# ws://localhost:8000/ws when VITE_WS_URL is unset.
#
# For production, set this in Vercel's dashboard (Project Settings ->
# Environment Variables), pointing at your deployed Render backend:
VITE_WS_URL=wss://your-backend.onrender.com/ws